function finder(str) {
    let sorted = [0]
    for(let i = 0; i < str.length; i++) {
        if(str[i].length > temp.length){
            temp[i] = str[i]
            // console.log(temp[i])
        }
        // console.log(str.length)
        
    }
}

finder(["open", "source", "programming", "is", "fun"])